import { LogoutButton } from './Logout.styled';

export const Logout = () => {
  return (
    <LogoutButton>
      {/* <svg></svg> */}
      Log Out
    </LogoutButton>
  );
};

export default Logout;
